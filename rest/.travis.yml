language: ruby
script: bundle exec /usr/bin/ruby test/all.rb
before_install:
  - sudo apt-get install ruby ruby-dev ruby-ldap ruby-bundler
notifications:
  email:
    - dev@opinsys.fi
    - esa-matti@suuronen.org
  irc: "irc.freenode.org#opinsys"
branches:
  only:
    - master
