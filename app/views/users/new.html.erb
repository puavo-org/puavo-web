<% page_title t('titles.schools'), @school.displayName, t('titles.users'), t('titles.new') %>

<h1><span><%= t('.title') %></span></h1>

<%= render :partial => 'form',
 :locals => {
  :path => users_path(@school),
  :submit_text => t('link.create'),
  :method => :post
}
%>
