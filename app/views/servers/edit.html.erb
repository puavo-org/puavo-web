<% page_title t('titles.organisation'), t('titles.servers'), @server.cn, t('titles.edit') %>

<div class="formContainer">

<h1><%= t('.title') %></h1>

<%= render partial: 'form',
    locals: {
      submit_text: t('link.update')
    }
%>

</div>

<%= render partial: 'shared/puavoconf_editor_templates' %>
<%= render partial: 'shared/puavoconf_editor', locals: { storage: 'server_puavoConf' } %>

<% if @image_filenames_by_release %>
<%= render partial: 'shared/known_recent_releases_selector', formats: :js, locals: { edit_field: 'server_puavoDeviceImage' } %>
<% end %>
