<% page_title t('titles.schools'), t('titles.new') %>

<h1><span><%= t('.title') %></span></h1>

<%= render :partial => 'form',
 :locals => {
  :path => schools_path,
  :submit_text => t('link.create'),
  :method => :post
}
%>
