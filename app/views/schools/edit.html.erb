<% page_title t('titles.schools'), @school.displayName, t('titles.edit') %>

<div class="formContainer">

<h1><%= t('.title') %></h1>

<%= render :partial => 'form',
 :locals => {
  :path => school_path(@school),
  :submit_text => t('link.update'),
  :method => :put
}
%>

</div>

<%= render partial: 'shared/puavoconf_editor_templates' %>
<%= render partial: 'shared/puavoconf_editor', locals: { storage: 'school_puavoConf' } %>

<% if @image_filenames_by_release %>
<%= render partial: 'shared/known_recent_releases_selector', formats: :js, locals: { edit_field: 'school_puavoDeviceImage' } %>
<% end %>
