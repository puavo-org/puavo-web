<h1><%= t('.title') %></h1>

<%= render :partial => 'form',
 :locals => {
  :path => school_path(@school),
  :submit_text => t('link.update'),
  :method => :put
}
%>
<div class="cancel"><div><%= link_to t('link.cancel'), @school, :class => 'cancel_button' %> </div></div>

