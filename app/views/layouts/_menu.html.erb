    <div id="tabs">
      <div class="first">
        <ul>
        <% @menu_items.each do |item| %>
        <% next if item.hide? %>
        <% if item.active? %>
          <li class="current">
        <% else %>
          <li>
        <% end %>
            <%= link_to item.link do %><span><%= item.title %></span><% end %>
          </li>
        <% end %>
        </ul>
      </div>

      <div class="second">
        <ul>
        <% @child_items.each do |item| %>
        <% next if item.hide? %>
        <% if item.active? %>
          <li class="current">
        <% else %>
          <li>
        <% end %>
        <% if item.owners_only %>
            <%= link_to item.title, item.link, class: 'ownersOnlyTop' %>
        <% else %>
            <%= link_to item.title, item.link %>
        <% end %>
          </li>
        <% end %>
        </ul>
      </div>
    </div>
