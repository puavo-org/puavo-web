<% page_title t('titles.schools'), @school.displayName, t('titles.devices') %>

<% if test_environment? %>
<%= render partial: 'index_legacy' %>
<% else %>
<%= render partial: 'index_new' %>
<% end %>
