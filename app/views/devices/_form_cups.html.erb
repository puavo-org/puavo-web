<h2><span><%= t('.cups_settings') %></span></h2>
<table>
  <tr>
    <th><%= f.label :printerDescription %></th>
    <td><%= f.object.attributes.include?("printerDescription") ? (f.text_field :printerDescription) :(text_field_tag  "device[printerDescription]", '', :size => "30", :readonly => true) %></td>
  </tr>

  <tr>
    <th><%= f.label :printerURI %></th>
    <td><%= f.object.attributes.include?("printerURI") ? (f.text_field :printerURI) : (text_field_tag "device[printerURI]", '', :size => "30", :readonly => true) %></td>
  </tr>

  <tr>
    <th><%= f.label :printerLocation %></th>
    <td><%= f.object.attributes.include?("printerLocation") ? (f.text_field :printerLocation) : (text_field_tag "device[printerLocation]", '', :size => "30", :readonly => true) %></td>
  </tr>

  <tr>
    <th><%= f.label :printerMakeAndModel %></th>
    <td><%= f.object.attributes.include?("printerMakeAndModel") ? (f.text_field :printerMakeAndModel) : (text_field_tag "device[printerMakeAndModel]", '', :size => "30", :readonly => true) %></td>
  </tr>

  <tr>
    <th><%= f.label :puavoPrinterCartridge %></th>
    <td><%= f.text_field :puavoPrinterCartridge %></td>
  </tr>
</table>
