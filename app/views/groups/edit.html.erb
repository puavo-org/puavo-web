<h1><%= t('.title') %></h1>

<%= render :partial => 'form',
 :locals => { :path => group_path(@school, @group),
  :method => :put,
  :submit_text => t('link.update')
} %>

<div class="cancel"><div><%= link_to t('link.cancel'), group_path(@school, @group), :class => 'cancel_button' %> </div></div>
