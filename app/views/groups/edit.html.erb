<% page_title t('titles.schools'), @school.displayName, t('titles.groups'), @group.displayName, t('titles.edit') %>

<h1><span><%= t('.title') %></span></h1>

<%= render :partial => 'form',
 :locals => { :path => group_path(@school, @group),
  :method => :put,
  :submit_text => t('link.update')
} %>
