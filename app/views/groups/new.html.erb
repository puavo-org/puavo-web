<% page_title t('titles.schools'), @school.displayName, t('titles.groups'), t('titles.new') %>

<h1><span><%= t('.title') %></span></h1>

<%= render :partial => 'form',
           :locals => {
             :path => groups_path(@school),
             :method => :post,
             :submit_text => t('link.create')
            } %>
