<table class="list full-width-list">
  <tr>
  <td class="label_td view"><%= label "external_service", "uid" %></td>
  <td class="value_td"><%= @external_service.uid %></td>
  </tr>

  <tr>
  <td class="label_td view"><%= label "external_service", "description" %></td>
  <td class="value_td"><%= @external_service.description %></td>
  </tr>

  <tr>
  <td class="label_td view"><%= label "external_service", "dn" %></td>
  <td class="value_td"><%= @external_service.dn.to_s %></td>
  </tr>

  <tr>
  <td class="label_td view"><%= label "external_service", "groups" %></td>
  <td class="value_td">
    <% @external_service.groups.each do |group| %>
    <div><%= group.description %></div>
    <% end %>
  </td>
  </tr>
</table>

<% content_for :right do %>
<div><%= link_to t('link.edit'), edit_external_service_path(@external_service) %></div>
<div>
  <%= link_to( t('link.destroy'),
               external_service_path(@external_service),
               :confirm => t('destroy_confirm',
                             :object_name => @external_service.uid),
               :method => :delete ) %>
</div>
<% end %>

<% content_for :footer do %>
<ul>  
  <li class="link_header">
    <%= link_to t('layouts.application.schools'),  schools_path %> 
  </li>
  <% school_list.each do |school| %>
  <li<% if school_list.last == school %> class="last"<% end %>>
    <%= link_to school.displayName,  school_path(school) %></li>
  <% end %>
</ul>
<% end %>
