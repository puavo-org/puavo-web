<% page_title t('titles.organisation'), t('titles.ldap_services'), @ldap_service.uid, t('titles.edit') %>

<div class="formContainer">

<h1><%= t('.title', service: @ldap_service.uid) %></h1>

<%= render partial: 'form',
    locals: {
      path: ldap_service_path(@ldap_service),
      submit_text: t('link.update'),
      method: :put
    }
%>

</div>
